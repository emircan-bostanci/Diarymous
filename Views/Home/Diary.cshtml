@model Diarymous.Models.Entities.Diary
@{
    Layout = "../_Layout.cshtml";
}
<script src="~/js/like.js"></script>
<div class="container-fluid">
    <div class="row content justify-content-center align-items-center">
        <div class="col-md-8 col-sm-12">
            <div class="col-lg-12">
                <h5>@Model.title </h5>
            </div>
            @Model.diaryText
            @{/*
            <div class="col-md-12">
                <div onclick="likeContent(@Model.id,'fa-heart')" class="row justify-content-between">
                    @if (ViewBag.like == true) {
                        <i class="text-danger fa  fa-solid fa-heart"></i>
                     }
                    @else
                    {
                        <i class="fa  fa-solid fa-heart"></i>
                </div>
            </div>
            */
        }
       </div>
    </div>
</div>
